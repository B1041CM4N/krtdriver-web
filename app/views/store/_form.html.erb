<%= form_for :store, method: method, url: url do |f| %>
  <div class="col-xs-12 col-md-6">
    <div class="form-group">
      <%= f.label "Nombre" %>
      <%= f.text_field :Name, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label "Dirección" %>
      <%= f.collection_select :Address_id, Address.all.order(Street_name: :asc), :Address_id, :Street_name, {}, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label "Descripción" %>
      <%= f.text_area :Description, min: 10, max: 1000, class: "form-control" %>
    </div>
    <%= f.submit "Enviar", class: "btn btn-primary btn-md" %>
    <%= link_to "Volver", store_index_url, class: "btn btn-secondary btn-md" %>
  </div>
<% end %>