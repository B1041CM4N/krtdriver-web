<div class="row">
  <div class="col-xs-12 col-md-12">
    <span id="notice"><%= notice %></span>
    <span id="alert"><%= alert %></span>
    <h3>Editar Tienda - <%= @store.name.capitalize %></h3>
  </div>
</div>
<%= render partial: "form", locals: { url: store_url, method: :put} %>