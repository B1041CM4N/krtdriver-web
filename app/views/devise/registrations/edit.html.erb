<h2>Perfil: <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>

  <div class="form-group col-xs-12 col-md-6 field">
    <%= f.label :email, "Correo Electrónico" %><br />
    <%= f.email_field :email, class: "form-control", autofocus: true, required: true %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Está esperando confirmación para la cuenta: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="form-group col-xs-12 col-md-6 field">
    <%= f.label :password, "Contraseña" %> <i>(Dejar en blanco si no la desea cambiar. Aplica también a su confirmación)</i><br />
    <%= f.password_field :password, class: "form-control", autocomplete: "off" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> caracteres mínimo</em>
    <% end %>
  </div>

  <div class="form-group col-xs-12 col-md-6 field">
    <%= f.label :password_confirmation, "Confirmación de contraseña" %><br />
    <%= f.password_field :password_confirmation, class: "form-control", autocomplete: "off" %>
  </div>

  <div class="form-group col-xs-12 col-md-6 field">
    <%= f.label :current_password, "Ingrese su contraseña actual" %> <i>(Necesitamos su contraseña para confirmar su identidad)</i><br />
    <%= f.password_field :current_password, class: "form-control", autocomplete: "off", required: true %>
  </div>

  <div class="actions">
    <%= f.submit "Guardar Cambios" %>
  </div>
<% end %>

<!-- 
  <h3>Darse de baja</h3>

  <p>Unhappy? <%#= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

  <%#= link_to "Back", :back %>
-->